Drop your song here as `theme-song.mp3` (e.g. `public/audio/theme-song.mp3`).

The floating play button in the bottom-left corner of the site will pick it up
automatically — no code changes needed. Until a file is added, the button
shows a muted music icon.
